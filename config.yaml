proxy:
  host: "localhost"
  port: 8080

cors:
  enabled: true
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:3001"
    - "https://reportes.syncestudio.com"
    - "https://ima-cloud.syncestudio.com"
  allowed_methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "PATCH"
    - "OPTIONS"
  allowed_headers:
    - "Content-Type"
    - "Authorization"
    - "x-user-id"
    - "x-user-name"
    - "x-user-role"
    - "X-Correlation-Id"
    - "Idempotency-Key"
  exposed_headers: []
  allow_credentials: true
  max_age: 86400

# auth:
#   default_provider: "auth-service"

#   providers:
#     auth-service:
#       type: "jwks"
#       jwks_url: "http://auth-service.railway.internal:8080/auth/jwks"
#       refresh_interval: 1h
#       issuer: "" # Optional but recommended

services:
  - url: "http://localhost:8081"
    alias: /warehouse
    strip_prefix: false
    auth:
      enabled: false
    rate_limit:
      requests: 200
      window: 1m
  - url: "http://localhost:8082"
    alias: /reports
    strip_prefix: false
    auth:
      enabled: false
    rate_limit:
      requests: 200
      window: 1m
  - url: "http://localhost:4000"
    alias: /api
    strip_prefix: false
    auth:
      enabled: false
    rate_limit:
      requests: 400
      window: 1m
